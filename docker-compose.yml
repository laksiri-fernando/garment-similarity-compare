version: '3.8'

services:
  garment-matcher:
    build: .
    container_name: garment-matcher
    ports:
      - "8000:8000"
    volumes:
      - ./samples:/app/samples
      - ./index:/app/index
    deploy:
      resources:
        limits:
          memory: 4G
    restart: unless-stopped
